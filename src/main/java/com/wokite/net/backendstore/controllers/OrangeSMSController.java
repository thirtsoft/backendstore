package com.wokite.net.backendstore.controllers;

import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.RestController;

@CrossOrigin
@RestController
public class OrangeSMSController /* implements OrangeSMSApi */ {

    /*
    @Autowired
    private OrangeSMSService orangeSMSService;

    public void sendSMS(MessageModel messageModel) {
        orangeSMSService.sendSMS(messageModel.getMobile(), messageModel.getMessage());
    }
    */

}
